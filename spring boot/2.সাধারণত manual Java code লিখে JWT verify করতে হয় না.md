না, সাধারণত **manual Java code লিখে JWT verify করতে হয় না**।
Spring ecosystem ব্যবহার করলে **framework নিজেই verification করে দেয়**।

এখানে সাধারণত ব্যবহার করা হয়:

* **Spring Boot**
* **Spring Security**
* **Keycloak**

---

# 1️⃣ Manual JWT Verify (যেটা সাধারণত করা হয় না)

যদি তুমি pure Java দিয়ে করো, তাহলে করতে হবে:

* JWT decode
* public key fetch
* signature verify
* expiry check

Example (manual):

```java
JWTVerifier verifier = JWT.require(Algorithm.RSA256(publicKey, null))
        .build();

DecodedJWT jwt = verifier.verify(token);
```

এটা **production microservice এ normally লেখা হয় না**।

---

# 2️⃣ Spring Boot Automatic Verification

Spring Boot + Spring Security দিলে **এক লাইন configেই কাজ হয়ে যায়**।

### application.yml

```yaml
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/banking
```

এটা দিলে Spring Boot automatically:

1️⃣ Keycloak public key fetch করবে
2️⃣ JWT verify করবে
3️⃣ expiry check করবে
4️⃣ issuer check করবে

---

# 3️⃣ Security Config Example

```java
@Configuration
@EnableMethodSecurity
public class SecurityConfig {

    @Bean
    SecurityFilterChain filterChain(HttpSecurity http) throws Exception {

        http
            .authorizeHttpRequests(auth -> auth
                    .requestMatchers("/public/**").permitAll()
                    .anyRequest().authenticated()
            )
            .oauth2ResourceServer(oauth -> oauth
                    .jwt()
            );

        return http.build();
    }
}
```

এখানে:

```java
.oauth2ResourceServer().jwt()
```

এই লাইনটাই **token verification enable করে**।

---

# 4️⃣ Role Based Access

Keycloak role ব্যবহার করতে পারো।

```java
@PreAuthorize("hasRole('MANAGER')")
@PostMapping("/loan/approve")
public String approveLoan() {
    return "approved";
}
```

Spring Security automatically:

* token read করবে
* roles extract করবে
* access check করবে

---

# 5️⃣ Runtime Flow

```text
Client
   │
   ▼
Authorization: Bearer token
   │
   ▼
Spring Security Filter
   │
   ▼
JWT verify
   │
   ▼
Controller execute
```

---

# 6️⃣ Microservice Architecture

```text
Keycloak
   │
   ▼
JWT Token
   │
   ▼
Gateway
   │
   ▼
User Service
Account Service
Payment Service
```

সব service একইভাবে verify করে — **manual code ছাড়াই**।

---

✅ **Short Answer**

❌ Manual JWT verify code লাগবে না
✔ Spring Security automatically verify করে

শুধু:

```yaml
issuer-uri
```

configure করলেই হয়।

---

যদি চাও, আমি তোমাকে দেখাতে পারি:

**🔥 Spring Boot + Keycloak Banking Project Structure (Gateway + 4 Microservices)**

এটা দেখলে enterprise architecture একদম clear হয়ে যাবে।
